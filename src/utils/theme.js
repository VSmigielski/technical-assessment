import { createMuiTheme } from '@material-ui/core' 

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#B8D2C2'
        },
        secondary: {
            main: '#FF5A5A'
        },
        type: 'dark'
    }
})

export default theme;